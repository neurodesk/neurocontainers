{
  "name": "Neurocontainers Build Environment",
  "postCreateCommand": "python3.13 -m venv --clear env && env/bin/python -m pip install --upgrade pip && env/bin/python -m pip install -r requirements.txt && env/bin/python -m pip install -e .",
  "image": "mcr.microsoft.com/devcontainers/python:3.13",
  "privileged": true,
  "features": {
    "ghcr.io/devcontainers/features/docker-outside-of-docker:1": {
      "moby": false
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "h5web.vscode-h5web"
      ]
    }
  },
  "remoteEnv": {
    "LOCAL_WORKSPACE_FOLDER": "${localWorkspaceFolder}",
    "PATH": "${localWorkspaceFolder}/env/bin:${localEnv:PATH}:${localWorkspaceFolder}/builder"
  }
}
