tests:
  - name: Test HCP-ASL entry points
    script: |
      python - <<'PY'
      import hcpasl
      print(hcpasl.__version__)
      PY
      process_hcp_asl --help
      get_sebased_bias_asl --help
      mt_estimation_asl --help
      command -v results_to_mni_asl
      command -v VolumetoSurfaceASL.sh
