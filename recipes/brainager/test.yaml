tests:
  #test if all packages are installed correctly. This should not give an error:
  - name: Test brainager
    manual: false
    script: |
      brainager_segment.py /opt/brainageR/chris_t1.nii.gz 
      predict_age.py --subjname chris_t1 /opt/brainageR/chris_t1